extends layout 

block content 
  h1= title 

  ul 
    each weapon in list 
      li 
        a(href=weapon.url) #{weapon.name}

        ul
          li #[strong Type]: #{weapon.type.name}
          li #[strong Manufacturer]: #{weapon.manufacturer.name}
          li #[strong Elements]:
          if weapon.element
            each element in weapon.element 
              | #{element.name} 
          else 
            | None
          li #[strong Rarity]: #{weapon.rarity.name}

    else 
      li There are no weapons.