extends layout

block content 
  h1= title 

  h2= detail.type.name 

  p. 
    #{detail.type.description}

  h3 Weapons with #[em #{detail.type.name}] type
  ul
    each weapon in detail.weapons 
      li #{weapon.name}