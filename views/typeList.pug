extends layout

block content 
  h1= title 

  ul 
    each type in typeList 
      li 
        a(href=type.url) #{type.name}
        if type.description 
          p #{type.description}

    else 
      li There are no types.